# Node.js Profiles

`core` defines native ESM TypeScript, GTS, ESLint, Prettier, typed factory
services, and Jest tests. Department-owned Node.js implementations depend on
this foundation and start from `templates/department-profile`.
