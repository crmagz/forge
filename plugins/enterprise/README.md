# Enterprise Plugins

Enterprise-level plugins apply across departments. `governance` resolves a
repository's profile stack; `foundation` supplies shared planning, adjudication,
and Git workflow standards.
