# Data Science Python Profiles

This is the ownership boundary for data-science Python implementations, such
as `dsmle`. A DSMLE profile should depend on `engineering-python-core` and own
its data contracts, reproducibility, model evaluation, and experiment-tracking
rules without inheriting SRE service layout by default.
